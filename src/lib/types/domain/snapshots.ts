// src/lib/types/domain/snapshots.ts
// ────────────────────────────────────────────────────────────
// Domain types for estimate snapshots.
//
// TODO: Populate after supabase gen types in Phase 1A-10
// Full types defined in type-system-research.md:
//   - SnapshotNode (branded Readonly<NodeWithDetails>)
//   - SnapshotData (JSONB payload with schemaVersion)
//   - EstimateSnapshot (snapshot metadata + data)
//   - SnapshotNodeRecord, SnapshotItemDetails, SnapshotAssemblyDetails
//   - SnapshotNoteRecord, SnapshotOptionGroupRecord
//   - SnapshotOptionAlternativeRecord, SnapshotOptionMembershipRecord
// ────────────────────────────────────────────────────────────

/** Placeholder -- will become the JSONB snapshot payload shape */
export type SnapshotData = unknown;

/** Placeholder -- will become the full snapshot metadata + data */
export type EstimateSnapshot = unknown;
