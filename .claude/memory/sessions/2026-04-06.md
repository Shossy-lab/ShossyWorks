# Session: 2026-04-06

## Summary
Pre-implementation planning session focused on build sequencing strategy and schema-impacting decisions. Confirmed data-first approach (build schema before UI). Identified 6 areas of user interaction decisions that affect database design, ranked by difficulty to change later. Began working through decisions starting with highest-risk "reshaping" items. Reached a conclusion on the notes architecture (multiple entries with rich text vs. single text field). Provided Zac a complete plain-language walkthrough of the core architecture.

## What Was Done
- Confirmed build sequencing: data architecture before UI (already aligned with INTENT.md principle #1 and implementation sequence)
- Identified 6 areas of interaction decisions needed before Phase 1A:
  1. Project & estimate lifecycle (status workflows)
  2. Node actions (duplicate, copy, convert)
  3. User preferences & saved views
  4. What gets attached to nodes (notes, files, comments)
  5. Client experience (what clients can do)
  6. Search & finding things
- Ranked decisions by difficulty to change later (reshaping > new table > new column)
- **Notes architecture decision:** Multiple note entries per node (`node_notes` table) with rich text support, rather than single text field. Each entry tracks author + timestamp. Soft-delete for version history. Zac confirmed this direction.
- Provided complete plain-language architecture walkthrough (tree, auto-promotion, calculations, catalog, options, versions, multi-user, formulas)
- Confirmed version history system is thoroughly covered (deep-copy snapshots + field-level triggers)
- Saved user profile and Figma prototypes to global memory

## Discoveries
- Zac reasons well about data modeling and system behavior at a conceptual level despite not being a developer -- can engage deeply with architectural trade-offs when explained in plain language
- The notes/comments distinction is the highest-risk "reshaping" decision for Phase 1A -- getting it wrong means migrating data later
- Zac confirmed notes are "to self" or "for the record" not conversations -- but wants multiple entries, rich text, and full history tracking

## Known Issues
- No new issues discovered this session
- Previous known issues still apply (Custom Access Token Hook not enabled, middleware deprecation)

## Next Steps
1. **Continue interaction decisions** -- 5 remaining areas to work through:
   - Project & estimate lifecycle (status workflows) -- next up
   - Node actions (duplicate, copy between estimates, convert types)
   - User preferences & saved views
   - Client experience (comments? approvals? share links?)
   - Search & finding things
2. **Then start Phase 1A** -- schema, constraints, triggers, server actions
3. `node_notes` table needs to be added to the Phase 1A migration plan

## Handoff Notes
- Repo is clean, no code changes this session (planning only, plus session docs)
- Branch: main
- Conversation was about to continue working through the 6 decision areas in order of risk
- We completed Decision A (notes vs. conversations) -- answer: `node_notes` table with rich text
- Decision areas 1-6 still need to be worked through before starting Phase 1A
- Zac is picking this conversation up on a different computer
